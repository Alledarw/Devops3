apiVersion: v1
kind: Service
metadata:
  name: web-server-nginx-service
spec:
  selector:
    app: web-server-nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort
